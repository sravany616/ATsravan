---
- hosts: localhost
  connection: local
  tasks:
    - name: Delete a resource group
      azure_rm_resourcegroup:
        name: AnsiTowerWinRG25
        force_delete_nonempty: true
        state: absent
